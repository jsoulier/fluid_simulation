{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "win32",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/fluid_simulation.exe",
            "args": [
                "${workspaceFolder}/tests/single_density_and_velocity.json"
            ],
            "cwd": "${workspaceFolder}/build/bin",
            "preLaunchTask": "CMake: build"
        },
    ]
}