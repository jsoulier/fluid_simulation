#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct type_UniformBuffer
{
    float Value;
};

kernel void main0(constant type_UniformBuffer& UniformBuffer [[buffer(0)]], texture3d<float, access::write> writeImage [[texture(0)]], uint3 gl_GlobalInvocationID [[thread_position_in_grid]])
{
    uint3 _24 = uint3(writeImage.get_width(), writeImage.get_height(), writeImage.get_depth());
    if (all(int3(gl_GlobalInvocationID) < int3(int(_24.x), int(_24.y), int(_24.z))))
    {
        writeImage.write(float4(UniformBuffer.Value), uint3(uint3(int3(gl_GlobalInvocationID))));
    }
}

